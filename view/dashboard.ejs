<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* General body styling */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #acacf3; /* Deep dark background */
            color: #e7dfdd; /* Light text color */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        /* Header section */
        header {
            width: 100%;
            padding: 20px 0;
            background-color: #acacf3; /* Vivid purple */
            box-shadow: 0 2px 4px acacf3(0, 0, 0, 0.3);
            text-align: center;
            margin-bottom: 40px;
            animation: slideInDown 1s ease-out; /* Header animation */
        }

        h1.heading {
            margin: 0;
            font-size: 2.5rem;
            color: #e7dfdd; /* Light color for heading */
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* Section styling */
        section {
            width: 80%;
            max-width: 1200px;
            background-color: #accdc9; /* Darker purple */
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            animation: fadeInUp 1.2s ease-out;
        }

        /* Form styling */
        section form {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-bottom: 20px;
            animation: pulse 1.5s infinite;
        }

        form input[type="text"] {
            background-color: #accdc9; /* Dark input background */
            color: #ffffff; /* Light text color */
            border: 1px solid #201248; /* Border matching the theme */
            padding: 10px;
            border-radius: 5px;
            width: 300px;
            margin-bottom: 10px;
            transition: box-shadow 0.3s ease;
        }

        form input[type="text"]:focus {
            box-shadow: 0 0 8px rgba(71, 23, 246, 0.8); /* Glowing focus effect */
        }

        form button {
            background-color: #ff9800; /* Bright orange button */
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        form button:hover {
            background-color: #e58900; /* Darker orange on hover */
            transform: translateY(-2px);
        }

        .dashboard-button {
            background-color: #ce48da; /* Bright cyan button */
            color: #fff;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }
        .logout-button{
            /* background-color: #ce48da; Bright cyan button */
            /* color: #fff; */
            padding: 10px 15px;
            text-decoration: none;
            /* border-radius: 5px; */
            /* transition: background-color 0.3s ease, transform 0.3s ease; */
            float: right;
            margin-right: 8.5%;
            font-size: 18px;
            color: white;
            border: none;
            border-radius: 8px;
            background: linear-gradient(135deg, #fc4a1a, #f7b733);
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .dashboard-button:hover {
            background-color: #3a4c4e;
            transform: translateY(-2px);
        }

        .dashboard-button + .dashboard-button {
            margin-left: 10px;
        }

        /* Table styling */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #7067ab; /* Dark purple background for table */
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #170e3a; /* Border matching theme */
        }

        th {
            background-color: #ce48da; /* Vivid purple header */
            color: #ffffff; /* White text */
            text-transform: uppercase;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #3e3b58; /* Darker purple for even rows */
        }

        tr:hover {
            background-color: #4e4a75; /* Slightly lighter on hover */
        }

        /* Update and Delete button styling */
        .update-button, .delete-button {
            padding: 8px 12px;
            border-radius: 5px;
            font-size: 0.9rem;
            text-decoration: none;
            color: #fff;
            display: inline-block;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .update-button {
            background-color: #4caf50; /* Green update button */
        }

        .update-button:hover {
            background-color: #388e3c;
            transform: translateY(-2px);
        }

        .delete-button {
            background-color: #f44336; /* Red delete button */
            border: none;
            cursor: pointer;
        }

        .delete-button:hover {
            background-color: #d32f2f;
            transform: translateY(-2px);
        }

        /* No data row */
        .no-data {
            text-align: center;
            color: #888;
            font-size: 1.1rem;
        }

        /* Keyframe animations */
        @keyframes slideInDown {
            from {
                transform: translateY(-100%);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                transform: translateY(20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

    </style>
</head>
<body>
    <header>
        <h1 class="heading">Admin Dashboard</h1>
        <a href="/user" class="logout-button">log out</a>
    </header>
  
    <section>
        <div> 
            <form action="/search" method="GET">
                <span><input type="text" name="_id" placeholder="Enter User ID" required>
                <button type="submit">Search</button></span>
            </form>
            <div class="buttons">
                <a href="/" class="dashboard-button">Go to Dashboard</a>
                <a href="/add" class="dashboard-button">Add User</a>
            </div>
        </div><br><br>
      
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Age</th>
                    <th>Power</th>
                    <th>Hobbies</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (data && data.length > 0) { %>
                    <% data.forEach(user => { %>
                        <tr>
                            <td><%= user._id %></td>
                            <td><%= user.name %></td>
                            <td><%= user.age %></td>
                            <td><%= user.power %></td>
                            <td><%= user.hobbies %></td>
                            <td>
                                <!-- Update Button: Navigates to update page -->
                                <a href="/update/<%= user._id %>" class="update-button">Update</a>

                                <!-- Delete Form -->
                                <form action="/delete/<%= user._id %>" method="POST" style="display:inline;">
                                    <button type="submit" class="delete-button">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="6" class="no-data">No user data available</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </section>
</body>
</html>
